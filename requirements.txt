# RetailIntel — local development dependencies
#
# Python 3.12 and JDK 17 are required. PySpark 3.5.x does not support Python
# 3.13+, and Spark 3.5 requires Java 17 or 11.
#
#   /opt/homebrew/bin/python3.12 -m venv .venv
#   .venv/bin/python -m pip install -r requirements.txt
#   export JAVA_HOME=/opt/homebrew/opt/openjdk@17
#
# Versions are pinned to match Databricks Runtime 15.4 LTS as closely as
# possible, so behaviour verified locally is the behaviour that runs in Azure.

pyspark==3.5.3
delta-spark==3.2.1
pandas==2.2.3
numpy==2.0.2
pyarrow==17.0.0
pytest==8.3.4
